# Lightweight OCR Receipt Scanner - Production Requirements
# Core OCR libraries (lightweight versions)
pytesseract==0.3.10
opencv-python-headless==4.8.1.78
Pillow==10.0.1

# Essential image processing
numpy==1.24.4

# Text processing
python-dateutil==2.8.2

# Web framework
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7

# Utilities
requests==2.31.0

# Deep learning OCR (heavier, but significantly improves accuracy for complex receipts)
easyocr==1.7.1
torch==2.0.1
torchvision==0.15.2
